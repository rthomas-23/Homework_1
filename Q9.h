#pragma once
#include <map>
#include <vector>
#include <string>
using namespace std;

map<string, vector<double>> guess_engine(map<string, vector<double>> test_data, vector<double> w_trained);
